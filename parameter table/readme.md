Вот исправленная версия файла `README.md` с уточнённым местоположением инструкции. Я обновил ссылки и дополнил описание, чтобы избежать путаницы на каждом этапе.

***

### **Сравнение параметров СУБД между отчетами pgpro_pwr**

Данная инструкция описывает процесс формирования сравнительной таблицы параметров кластера PostgreSQL на основе двух отчетов `pgpro_pwr` (например, за разные периоды или с разных серверов).

#### **Необходимые файлы**
*   **Инструкция для нейросети**: `_prompt-instruction.txt` (доступна в [репозитории GitHub](https://github.com/pg-expecto/deepseek-pg-perf-prompts/blob/main/_prompt-instruction.txt))
>   Загрузите этот файл и сохраните локально — он потребуется на всех трех этапах.
*   **Шаблон для извлечения параметров**: `Таблица.G.78.txt`
*   **Отчеты `pgpro_pwr` в формате HTML**: `pgpro_pwr.X1-X2.clear.html` и `pgpro_pwr.Y1-Y2.clear.html`

---

#### **Этап 1 – Извлечение параметров из первого отчета**
1.  Откройте **новый чат** DeepSeek.
2.  Загрузите в чат файл **`_prompt-instruction.txt`** (локальная копия).
3.  Следуя инструкции, отредактируйте файл `prompt-correspondence_table.source.1.txt`:
    *   Замените плейсхолдер `X1-X2` на фактические идентификаторы снимков (например, `01-02` или `A-B`) из имени первого отчета `pgpro_pwr`.
4.  Загрузите в чат **первый отчет** `pgpro_pwr` (`pgpro_pwr.X1-X2.clear.html`) и файл **`Таблица.G.78.txt`**.
5.  Отправьте исправленный файл `prompt-correspondence_table.source.1.txt` как запрос.
6.  Сохраните полученный результат в файл `table.source.1.txt`.

#### **Этап 2 – Извлечение параметров из второго отчета**
1.  Откройте **новый чат** DeepSeek.
2.  Загрузите в чат файл **`_prompt-instruction.txt`** (локальная копия).
3.  Следуя инструкции, отредактируйте файл `prompt-correspondence_table.source.2.txt`:
    *   Замените плейсхолдер `Y1-Y2` на фактические идентификаторы снимков из имени второго отчета `pgpro_pwr`.
4.  Загрузите в чат **второй отчет** `pgpro_pwr` (`pgpro_pwr.Y1-Y2.clear.html`) и файл **`Таблица.G.78.txt`**.
5.  Отправьте исправленный файл `prompt-correspondence_table.source.2.txt` как запрос.
6.  Сохраните полученный результат в файл `table.source.2.txt`.

#### **Этап 3 – Формирование итоговой сравнительной таблицы**
1.  Откройте **новый чат** DeepSeek.
2.  Загрузите в чат файл **`_prompt-instruction.txt`** (локальная копия).
3.  Следуя инструкции, отредактируйте файл `prompt-correspondence_table.result.txt`:
    *   Замените `X1-X2` на идентификаторы снимков первого отчета (из этапа 1).
    *   Замените `Y1-Y2` на идентификаторы снимков второго отчета (из этапа 2).
4.  Загрузите в чат результаты предыдущих этапов:
    *   `table.source.1.txt`
    *   `table.source.2.txt`
5.  Отправьте исправленный файл `prompt-correspondence_table.result.txt` как запрос.
6.  Сохраните итоговый результат в файл `table.result.txt`.

#### **Примечания**
*   Все промежуточные файлы (`table.source.1.txt`, `table.source.2.txt`) сохраняются локально.
*   Итоговая таблица `table.result.txt` содержит только те строки параметров (Setting), для которых значения в двух отчетах различаются или отсутствуют в одном из них.
*   Формат итоговой таблицы: `Setting | отчет-X1-X2 | отчет-Y1-Y2`.

***

Основные изменения:
*   В начале добавлен раздел «Необходимые файлы» с прямой ссылкой на файл `_prompt-instruction.txt` в репозитории.
*   На каждом этапе теперь явно указано, что нужно загружать локальную копию этого файла.
*   Сами пункты шагов слегка переформулированы для большей ясности.
